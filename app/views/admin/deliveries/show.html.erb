<div class="row">
	<div class="col-lg-12">
		Название: <%= @delivery.title%><br>
		Стоимость: <%= @delivery.price%><br>
		<% unless @delivery.price_km == nil%>
		<%= @delivery.price_km%>
		<%end%>
	</div>
</div>
<div class="row">
	<%= link_to 'Назад', admin_deliveries_path%>
</div>